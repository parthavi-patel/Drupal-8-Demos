<?php
/**
 * Implements hook_theme().
 */
function jobform_theme( $existing, $type, $theme, $path ){

    return array (
	    'manage_jobform_display' => array (
		    'variables' => array(
			    'form_in_twig' => NULL
		      ),
	        'template' =>'manage-jobform',
	    ),
	);
}
